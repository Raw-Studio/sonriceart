<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/sketchy/bootstrap.min.css">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col">
                <a href="/admin/showcase"><button type="button" class="btn btn-primary btn-lg mb-5 mt-5"> Back to
                        Store list</button></a>
                <form method="POST" action="/admin/showcase/<%= method %>" enctype="multipart/form-data">
                    <input type="hidden" name="_csrf" value="<%= _csrf %>">
                    <% if(item !== ''){ %>
                    <input type="hidden" name="_id" value="<%= item._id %>">
                    <% } %>
                    <fieldset>
                        <% if(item === ''){ %>
                        <div class="form-group">
                            <label for="title" class="col-sm-2 col-form-label">Title</label>
                            <div class="col-sm-10">
                                <input type="text" name="title" class="form-control-plaintext" id="title"
                                    placeholder="Title">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="order" class="col-sm-2 col-form-label">Order</label>
                            <div class="col-sm-10">
                                <input type="number" name="order" class="form-control-plaintext" id="order"
                                    placeholder="Order">
                            </div>
                        </div>
                        <% } else{ %>
                        <div class="form-group">
                            <label for="title" class="col-sm-2 col-form-label">Title</label>
                            <div class="col-sm-10">
                                <input type="text" name="title" class="form-control-plaintext" id="title"
                                    placeholder="Title" value="<%= item.title %>" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="order" class="col-sm-2 col-form-label">Order</label>
                            <div class="col-sm-10">
                                <input type="number" name="order" class="form-control-plaintext" id="order"
                                    placeholder="Order" value="<%= item.order %>">
                            </div>
                        </div>
                        <% } %>
                        <% if(item !== ''){ %>
                        <img style="height: 200px; display: block;" src="<%= item.image %>" alt="Card image">
                        <% } %>
                        <div class="form-group">
                            <label for="exampleInputFile">Image</label>
                            <input type="file" class="form-control-file" id="exampleInputFile"
                                aria-describedby="fileHelp" name="cover">
                            <small id="fileHelp" class="form-text text-muted">This is some placeholder
                                block-level help text for
                                the above input. It's a bit lighter and easily wraps to a new line.</small>
                        </div>
                        <button type="submit" class="btn btn-primary">Add</button>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
</body>

</html>